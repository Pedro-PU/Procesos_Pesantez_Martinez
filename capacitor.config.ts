import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.pesantezpmartinezfprocesos',
  appName: 'pesantezpmartinezfprocesos',
  webDir: 'www'
};

export default config;
